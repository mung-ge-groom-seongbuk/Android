<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mungge_groom" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="CustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item> <!-- 원하는 크기 -->
        <item name="android:textStyle">bold</item> <!-- 원하는 스타일 (italic, normal 등) -->
    </style>
    <style name="fab_3_rounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="Theme.Mungge_groom" parent="Base.Theme.Mungge_groom" />

    <style name="NoBackgroundBottomNavigation" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@android:color/transparent</item>
        <item name="itemBackground">@android:color/transparent</item>
        <item name="itemRippleColor">@android:color/transparent</item>
    </style>

    <!-- res/values/styles.xml -->
    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicator">@drawable/custom_tab_indicator</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">16dp</item>
        <item name="tabPaddingEnd">16dp</item>
        <item name="tabBackground">@android:color/transparent</item>
        <item name="tabIndicatorFullWidth">false</item>
    </style>

    <style name="ToolbarTitleStyle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20dp</item>

    </style>

    <style name="ToolbarSubtitleStyle" parent="TextAppearance.MaterialComponents.Subtitle1">
        <item name="android:textColor">#FF922C</item>
        <item name="android:textSize">12dp</item>

    </style>

    <style name="DialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/DialogStyle</item>
    </style>
    <style name="DialogStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_fill_white_rounded_12dp</item>
    </style>

</resources>